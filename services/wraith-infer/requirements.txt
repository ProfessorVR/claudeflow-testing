# wraith-infer runtime deps. Install into a venv on WRAITH (see run.sh / README.md).
# Torch is intentionally unpinned here — install the CUDA build matching WRAITH's drivers
# FIRST (e.g. `pip install torch --index-url https://download.pytorch.org/whl/cu121`),
# THEN `pip install -r requirements.txt`.
sentence-transformers>=3.0.0
transformers>=4.41.0
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.6.0
numpy>=1.26.0
# gte-Qwen2 custom modeling code path:
einops>=0.7.0
