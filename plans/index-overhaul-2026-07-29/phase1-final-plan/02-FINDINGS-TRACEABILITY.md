# 02 — Findings traceability matrix (all 239 verified findings, workflows A–D)

Coverage: **239 verified findings** — A 93 (89 confirmed / 4 refuted) · B 63 (56/7) · C 43 (30/13) · D 40 (24/16). Every confirmed finding carries exactly one disposition: **Addressed** (numbered step in `01-FINAL-OVERHAUL-PLAN.md`), **Deferred** (with reason), **Rejected** (with reason), or **Rejected — component abandoned**. Every refuted finding is listed with a pointer to its refutation. No finding is omitted.

**Provenance of this matrix:** the verified-finding set was reconstructed from the four workflow journals (the verbatim per-agent record) using the selection logic in the archived workflow scripts (A: CRITICAL|HIGH, 8/batch × 12 batches; B: 7/scope × 9; C: 6/scope × 8; D: 5 load-bearing/assessor × 8). Workflow D’s journal contains 74 verifier records; the final 40 (the post-outage resume pass, exactly 16 refutations) are authoritative — the first 34 are the superseded pre-outage pass. Claim↔verdict pairing was rebuilt by content matching (verdict records do not name the finding they verified); pairing scores are logged in `phase1-worklog.md`. The finding text below is the ORIGINAL claim as filed; where the verifier corrected severity, the corrected severity is shown. Location cites resolve against the dirty working tree as frozen by step P0.2 (see 01 §AR1).

**Disposition summary:**

| Disposition | Count |
|---|---|
| Addressed by a numbered step | 173 |
| Explicitly deferred with reason | 26 |
| Explicitly rejected with reason | 0 |
| Rejected — component abandoned | 0 |
| **Confirmed total** | **199** |
| Refuted (listed with refutation pointers) | 40 |
| **Grand total** | **239** |

> Note on the empty “component abandoned” row: it was checked, not skipped. No verified finding’s **only** relevance is to WRAITH or to the archon agent-coding stack — the audit predated the reranker wiring’s audit scope, workflow D’s scopes did not cover `archon-coder`, and the wraith-adjacent findings that do exist (B-27 boost interplay; D-28/D-31 bug-compat traps) sit in shared code and are dispositioned on their own merits above/below. The removal work itself is steps A5/A6.


## Workflow A — every index entry (27 directories)

### Confirmed

| ID | Sev | Location / scope | Finding (as filed) | Disposition |
|---|---|---|---|---|
| A-01 | MEDIUM | corpus/index/Heidegger - Being and Time/bt-analysis/book-level-ontology.md:33 -> corpus/index/compil | The Dasein node's units field is authored as English prose, so the compiler tokenizes it into three junk unit IDs; BT's most central node reaches runtime with ZERO valid BT unit references. | **Addressed** — C5 imports tensions/nodes from structured sources with resolvable unit ids; C1 forbids prose-valued unit fields; F2 checklist (Being and Time) |
| A-02 | LOW | corpus/index/Heidegger - Being and Time/bt-analysis/tension-edges.json:59 vs bt-analysis/book-level- | tension-edges.json contains 9 tensions but §3C of the book-level ontology documents only 8; T9 has no prose counterpart anywhere. | **Addressed** — C5 zero-silent-drop import surfaces the T9/§3C mismatch; F2 checklist (BT) |
| A-03 | MEDIUM | corpus/index/Heidegger - Being and Time/bt-analysis/book-level-ontology.md:136-147 and :303-314 | The last node of each centrality tier has its centralityTier corrupted by the following tier header — same parser bug, three occurrences in this one file. | **Addressed** — A2 (elif reorder stops the tier-header spill at source); F2 checklist (BT) |
| A-04 | HIGH | corpus/index/Heidegger - Being and Time/bt-analysis/book-level-ontology.md:415-494 -> corpus/index/c | The last ontology node swallows the entire remainder of the file. Its compiled `units` array has 586 elements and its `centralityTier` is a 247-character garbage string. | **Addressed** — A2 (same mechanism — the 586-unit/247-char node); acceptance check in A2 asserts no such node after recompile |
| A-05 | MEDIUM | corpus/index/Boredom Secondary (Part III)/_synthesis/global-edges.csv (1,897 rows) vs the 55 per-uni | global-edges.csv is exactly the set of distinct (source, relation, target) triples and therefore silently discards the locus and note columns of 21 rows — i.e. it destroys precisely the passage-level evidence that distinguishes one edge from another. | **Addressed** — C5/§7-Phase-3 rule: aggregation is concatenation keyed on edge_id, never content-dedup; the 21 locus/note-bearing rows import from the per-unit layer; F2 (Boredom Secondary) |
| A-06 | MEDIUM | corpus/index/Heidegger - Being and Time/bt-analysis/tension-edges.json:1-66 | BT tension edges carry no `id`, no `label` and no `units` key, unlike the sibling BCAP file, so all 9 reach runtime with empty unit provenance. | **Addressed** — C5 TENSION schema absorbs the id-less BT shape and requires unit provenance; F2 (BT) |
| A-07 | MEDIUM | corpus/index/Boredom Secondary (Part III)/_synthesis/debate-map.json (axes DA-09, DA-10, DA-11) vs _ | 3 of the entry's 11 debate axes have no ontology node and therefore never reach compiled-index.json. | **Addressed** — C5 imports debate axes as GROUP-layer records independent of ontology-node existence; F2 (Boredom Secondary) |
| A-08 | MEDIUM | corpus/index/Heidegger - Being and Time/bt-analysis/global-edges.csv vs bt-analysis/phase2-bt-*.md ( | The per-unit and synthesis edge layers disagree: 290 distinct CONCEPT->CONCEPT triples per-unit vs 284 in global-edges.csv; 9 per-unit-only, 3 global-only. Nothing records which is stale. | **Addressed** — C5 imports BOTH layers and publishes the 290-vs-284 disagreement for resolution on the F2 checklist (BT) |
| A-09 | MEDIUM | corpus/index/Heidegger - Being and Time/bt-analysis/tension-edges.json:10,17,45,52,59 | 6 of the 18 tension endpoints name concepts that are not ontology nodes and not aliases. | **Addressed** — C1 EDGE/TENSION rule: unresolvable endpoints fail the build (check-ids/check-schema at D1); F2 (BT) |
| A-10 | MEDIUM | corpus/index/Heidegger - Being and Time/bt-analysis/book-level-ontology.md:22 vs bt-analysis/global- | Three unit-ID dialects inside one entry; the dialect that reaches runtime is the one that joins to nothing. | **Addressed** — C1 global id namespace + D1 check-ids; F2 (BT) |
| A-11 | MEDIUM | corpus/index/Boredom Secondary (Part III)/*/bor-sec-*.json — `readings` array | The claim-level array — the entry's most valuable structure — has 9 mutually incompatible sub-schemas across the 55 units. | **Addressed** — C1 CLAIM schema replaces the 9 incompatible readings sub-schemas at import (C5) and regeneration (F2, Boredom Secondary) |
| A-12 | MEDIUM | corpus/index/Boredom Secondary (Part III)/_synthesis/cluster-ontology.md:135 | The self-declared node type breakdown is wrong on two of five figures and omits an entire type. | **Addressed** — D1 manifest gate: declared == discovered, both directions; F2 (Boredom Secondary) |
| A-13 | LOW | 56 `shifts-debate-axis` rows across corpus/index/Boredom Secondary (Part III)/*/bor-sec-*-edges.csv | The debate-axis edge targets are uncontrolled free text with 46 distinct values and zero use of the DA-NN identifiers, so the edge layer cannot be joined to debate-map.json or to the 8 DEBATE-AXIS ontology nodes. | **Addressed** — C1: debate-axis targets become registry-resolved ids (DA-NN), free text illegal; C5 import maps the 46 variants; F2 |
| A-14 | HIGH | corpus/index/In-Game (Calleja 2011)/_synthesis/tension-edges.json:13 (start of `tensions` array) | All 12 volume-level tensions yield ZERO runtime edges, silently. Entry keyset is ('description','dissertation_implication','label','page_anchors','supporting_units','tension_id','type') — no node_a/node_b/nodeA/nodeB. | **Addressed** — C5 gate names this file: In-Game 12 tensions must yield 12 records (schema absorbs tension_id/label/supporting_units shape); F2 (In-Game) |
| A-15 | HIGH | corpus/index/In-Game (Calleja 2011)/_synthesis/book-level-ontology.json (positions array, 18 entries | The 18 canonical POSITIONs exist only in the JSON twin. The .md §3A node list enumerates 44 blocks (38 concepts + 6 dimensions) and no positions; the compiler reads only the .md. | **Addressed** — C5 imports the 18 POSITIONs from book-level-ontology.json (structured JSON is the source of truth in the archon-native path); F2 (In-Game) |
| A-16 | MEDIUM | corpus/index/In-Game (Calleja 2011)/Calleja - From Immersion to Incorporation/ig-10-deep.json (`edge | Every deep JSON that self-reports a delivered edge count under-reports the actual CSV. Seven independent mismatches, zero agreements. | **Addressed** — D1 manifest/count gates (declared==discovered); F2 (In-Game) |
| A-17 | LOW | corpus/index/In-Game (Calleja 2011)/Calleja - From Immersion to Incorporation/ig-10-incorporation.js | Bracket-TYPE mismatch: line 134 opens `"non_game_studies_anchor_theories": {` (an object) but line 155 closes it with `],`. A single-character fix (`],` -> `},`) restores a valid 42-key document. | **Addressed** — A4 fixes the one-character bracket defect; acceptance = tree-wide 0 unparseable JSON |
| A-18 | MEDIUM | corpus/index/In-Game (Calleja 2011)/_synthesis/book-level-ontology.json:664-668 (`tier_distribution` | The declared tier distribution contradicts the per-node `tier` fields in the same file and the per-node `centrality` fields in the .md twin. | **Addressed** — D1 count gates; F2 (In-Game) |
| A-19 | LOW | corpus/index/In-Game (Calleja 2011)/_synthesis/tension-edges.json:15 vs _synthesis/book-level-ontolo | `tension_id` values T-001..T-012 collide head-on with the ontology's TEMPORAL-PHASE `canonical_id` values T-001 and T-002. | **Addressed** — C1 global id namespace with cross-entry collision report (D1 check-ids); F2 (In-Game) |
| A-20 | HIGH | corpus/index/In-Game (Calleja 2011)/Calleja - From Immersion to Incorporation/ig-10-deep-edges.csv:2 | 847 of 1,472 per-unit edge endpoints (58%) are not declared as an id/label/name anywhere in that unit's own JSON. Four units are at 100% — the edge CSV and the concept registry use entirely disjoint namespaces. | **Addressed** — C1 EDGE endpoints must resolve to declared ids, else build fails; C5 import quantifies the 847 dangling endpoints on the F2 checklist (In-Game) |
| A-21 | CRITICAL | scripts/ingest/manifest.jsonl (273 rows for corpus/index/Dissertation/, all .md) | The ingest pipeline indexed only markdown. Every .json/.csv/.jsonl in the entry — i.e. the whole machine-readable layer — was skipped. | **Addressed** — C4 imports the Dissertation machine-readable layer directly from disk (ingest-pipeline md-only skip is bypassed); A1 stops index re-embedding; F2 (Dissertation) |
| A-22 | MEDIUM | corpus/index/In-Game (Calleja 2011)/Calleja - Kinesthetic Involvement/ig-04-deep-edges.csv:1 (and 6  | Seven distinct header dialects across 13 per-unit edge CSVs, plus 3 more in _synthesis. Column names, order, and even arity vary (6 to 12 columns). | **Addressed** — C1 single canonical EDGE format with verbatim header gated on exact equality; C5 adapter normalizes the 7+3 dialects; F2 (In-Game) |
| A-23 | LOW | corpus/index/Dissertation/_run-history/2026-05-13T1439/_synthesis/citation-gap-master.json:40-49 | severity_distribution sums to 210 while the same file's total_gaps (:15) is 207. | **Addressed** — D1 count gates on generated views; F2 (Dissertation run-history artifacts — historical, low priority) |
| A-24 | HIGH | scripts/compile-corpus-index.py:35-133 (TEXT_DIRS) | 'Dissertation' is absent from TEXT_DIRS, so collect_ontology_nodes()/collect_cross_pipeline_hooks()/collect_tension_edges() never open the directory. Zero of its 358 files are read by the compiler. | **Addressed** — A7 transfer + C4 import read the directory from disk; registration-by-glob at D1 (check-registration) retires TEXT_DIRS; F2 (Dissertation) |
| A-25 | HIGH | corpus/index/Red Dead Redemption 2 (Salvo Playthrough)/rdr2-tutorial-RODA.md:34 vs :131 | The file's own clip ledger and its §8 closing synthesis assert opposite honor verdicts for clip 10. | **Addressed** — F2 checklist (RDR2 Playthrough): the clip-10 honor contradiction is a named content defect to resolve at regeneration |
| A-26 | MEDIUM | corpus/index/Dissertation/_run-history/2026-05-13T1439/_synthesis/master-citation-report-parsed.json | Declares 244 entries parsed at 100% success, but ships 235; and its own per-section breakdown totals 213. Three mutually exclusive counts in one file. | **Addressed** — D1 count gates; F2 (Dissertation run-history) |
| A-27 | HIGH | corpus/index/Dissertation/_run-history/2026-05-13T1439/_synthesis/master-citation-report-parsed.json | A cached verbatim marked `"use": "in-text"` (i.e. destined for the dissertation body) carries pdfium ligature loss: 'figures' rendered 'fgures'. | **Addressed** — E1 (pypdfium2 bump) fixes the extraction cause; C8 re-anchoring classifies the cached verbatim via B4; F2 (Dissertation) |
| A-29 | HIGH | corpus/index/Red Dead Redemption 2 (Salvo Playthrough)/rdr2-element-matrix.md:57 | The bear is still named the corpus's purest flinch specimen after the frame pass established that no bear charge/maul occurs. | **Addressed** — F2 checklist (RDR2 Playthrough): named refuted-claim-uncorrected defect; D4 reverse-reference index prevents recurrence |
| A-30 | HIGH | corpus/index/Red Dead Redemption 2 (Salvo Playthrough)/rdr2-element-matrix.md:71 | The bar-fight row still asserts the honor-ACTIVE reading and the Trelawney mis-ID that frame-read-verification.md refuted a month earlier. | **Addressed** — F2 checklist (RDR2 Playthrough), as A-29 |
| A-31 | HIGH | corpus/index/Red Dead Redemption 2 (Salvo Playthrough)/rdr2-element-matrix.md:89 and :90 | Two of the matrix's six numbered synthesis findings cite the non-existent bear maul/charge as evidence. | **Addressed** — F2 checklist (RDR2 Playthrough), as A-29 |
| A-32 | CRITICAL | corpus/index/Red Dead Redemption 2 (Salvo Playthrough)/rdr2-vs-gg-differential.md:13 and :21 | The differential file - the designated Part II drafting input - still anchors R2-embodiment on the bear and carries no verification banner at all. | **Addressed** — F2 checklist (RDR2 Playthrough): the Part II drafting input gets a verification banner and correction at regeneration; D4 prevents recurrence |
| A-33 | CRITICAL | scripts/compile-corpus-index.py:35-133 | Entry is absent from TEXT_DIRS, so collect_ontology_nodes() and collect_tension_edges() never open the directory. | **Addressed** — A7 + C imports read from disk; D1 check-registration; F2 (RDR2 Playthrough) |
| A-34 | MEDIUM | corpus/index/Dissertation/_run-history/2026-05-13T1439/_synthesis/master-citation-report-parsed.json | A cached 'verbatim' silently alters the source text: the White 1985 passage reads 'imagination (phantasia) is the process...' in the PDF but the cache stores 'phantasia is the process...' with no ellipsis or bracket. | **Addressed** — C8 re-anchoring flags the altered White 1985 verbatim (exact-match gate fails on silent elision); F2 (Dissertation) |
| A-35 | MEDIUM | corpus/index/Dissertation/_run-history/2026-05-20T0058/_per-section/*/phase2-claims.json (all 6) | Run 2's claim records are byte-identical copies of run 1's and still declare run_id '2026-05-13T1439', despite run 2's manifest asserting all seven inputs changed and +14,985 words were added. | **Addressed** — C1 build_provenance (agent, model, built_at, plan_sha) makes byte-identical stale copies detectable; F2 (Dissertation run-history) |
| A-36 | MEDIUM | corpus/index/Red Dead Redemption 2 (Salvo Playthrough)/frame-traces/*.md (22 files) | The correction layer is strictly one-directional: not one beat-map points back at the verification log that corrects it. | **Addressed** — D4 reverse-reference index makes corrections transactional in both directions; F2 (RDR2 Playthrough) |
| A-37 | LOW | corpus/index/Heidegger - The Fundamental Concepts of Metaphysics/units/fcm-02-prelim-ch3-physis-logo | Seven unit JSONs declare their working-text provenance under an ephemeral system-tmp path that no longer exists. | **Addressed** — C1 SOURCE.text_layer_id + committed `_source/` artifacts (§7 Phase 0) replace ephemeral /tmp provenance; F2 (FCM) |
| A-38 | MEDIUM | corpus/index/Heidegger - The Fundamental Concepts of Metaphysics/_synthesis/manifest.json:14 | page_offset declared 'verified: true' and gate 'triple_loci: PASS', but 21% of the entry's triple-loci are inconsistent with the verified formula. | **Addressed** — D5/§7 Phase-0 locator calibration (affine check, piecewise map); D1 check-loci round-trips; F2 (FCM) |
| A-40 | HIGH | corpus/index/Heidegger - The Fundamental Concepts of Metaphysics/_synthesis/global-edges.csv:1 (and  | All 880 authored edge rows are unreachable by the only compiler that feeds runtime. | **Addressed** — C5 imports the 880 FCM edge rows into reachable EDGE records; F2 (FCM) |
| A-41 | MEDIUM | corpus/index/Heidegger - The Fundamental Concepts of Metaphysics/_synthesis/manifest.json:59 | Declared quality gate 'no_verbatim_over_25w: PASS (anchor-phrases only, <=20w)' is false. | **Addressed** — C8 re-anchoring + Decision-1 rights-tier storage replace the false no_verbatim gate with a machine check; F2 (FCM) |
| A-42 | MEDIUM | corpus/index/Heidegger - The Fundamental Concepts of Metaphysics/_synthesis/global-edges.csv | 463 of 481 distinct edge endpoints resolve to no declared node in any node registry. | **Addressed** — C1 resolvable-endpoint rule; C5 quantifies the 463 danglers on the F2 checklist (FCM) |
| A-43 | MEDIUM | corpus/index/A Grammar of Motives (Burke 1945)/ (whole tree) | Not one of the 91 files is tracked by git; the entire entry is untracked working-tree content. | **Addressed** — P0.2 commits the 91-file Grammar tree (done at Phase 0) |
| A-44 | MEDIUM | corpus/index/Heidegger - The Fundamental Concepts of Metaphysics/_synthesis/book-level-ontology.json | The file the ontology .md calls 'machine-readable' is read by nothing, holds fewer nodes than the .md, and its 20 edges are 90% absent from the CSV layer. | **Addressed** — C5 treats the structured JSON as source of truth and imports its 20 edges; F2 (FCM) |
| A-45 | HIGH | corpus/index/A Grammar of Motives (Burke 1945)/_synthesis/tension-edges.json:14 | All 16 canonical tensions and all 21 unmatched raw tensions yield ZERO tensionEdges at compile time, silently and without a warning. | **Addressed** — C5 gate: Grammar 16 canonical + 21 raw tensions must import at full count; F2 (Grammar) |
| A-46 | HIGH | scripts/ingest/manifest.jsonl (1,182 rows) | D4's ratified promise that analysis-artifact entries are 'still reachable by entry-scoped retrieval' is false today: the entry is also absent from the vector store. | **Addressed** — C4 imports VdS 157 + RDR2 Playthrough records into archon (retrievable by construction); the false D4 ratification is retired (review §7); F2 |
| A-47 | HIGH | corpus/index/Heidegger - The Fundamental Concepts of Metaphysics/_synthesis/global-edges.csv | The same concept carries up to four distinct endpoint identities inside one CSV. | **Addressed** — C1 ids-only join rule + C5 endpoint resolution; F2 (FCM) |
| A-48 | HIGH | corpus/index/A Grammar of Motives (Burke 1945)/_synthesis/global-edges.csv:2-877 | 398 distinct relation values are used across 876 edges, 280 of them exactly once — the relation column is effectively free text, not a controlled vocabulary. | **Addressed** — §4.5 relation registry (seeded from the 645 observed values, `other` requires relation_raw + review flag); C5 maps Grammar’s 398; F2 |
| A-49 | MEDIUM | corpus/index/A Grammar of Motives (Burke 1945)/Burke - Act (Aristotle and Aquinas)/gm-06-deep.json:3 | Six of fourteen units carry self-reported edge totals that contradict their own CSV. | **Addressed** — D1 count gates; F2 (Grammar) |
| A-50 | MEDIUM | corpus/index/Aristotle - Complete Works/aristotle-bekker-index.md:5 | The bekker index declares 157 loci; both the .md and the .json contain 188. | **Addressed** — D1 count gates; F2 (Aristotle) |
| A-51 | MEDIUM | corpus/index/A Grammar of Motives (Burke 1945)/Burke - Antinomies of Definition/gm-02-deep.json:111, | gm-02 declares three mutually contradictory edge counts in three places. | **Addressed** — D1 count gates; F2 (Grammar) |
| A-52 | MEDIUM | corpus/index/A Grammar of Motives (Burke 1945)/Burke - Symbolic Action in a Poem by Keats/gm-10-deep | The pipeline's mandatory 'no ≥25-word verbatim from Burke' rule (plans/grammar-of-motives-burke-1945-analysis.md:247, :392) is violated in 31 places across the unit analyses; the longest is 97 words. | **Addressed** — C1 [DECISION 1] rights-tiered storage/display retires the blanket 25-word ban; the 31 violations become stored clauses with render policy; F2 (Grammar) |
| A-53 | MEDIUM | corpus/index/A Grammar of Motives (Burke 1945)/Burke - Antinomies of Definition/gm-02-deep-edges.csv | The 14 per-unit edge CSVs use 8 mutually incompatible header dialects with 5 different column counts. | **Addressed** — C1 single EDGE format, exact-header gate; F2 (Grammar) |
| A-54 | HIGH | corpus/index/Aristotle - Complete Works/aristotle-da-04.json:9 vs phase2-da-04.md:10 | The unit .json and its .md analysis twin disagree on pdf_pages for 32 of 34 units, and the .md is the one that is right. | **Addressed** — F2 checklist (Aristotle): pdf_pages regenerated from locator calibration; the .md twin is the correct source; D1 check-loci |
| A-55 | HIGH | corpus/index/Aristotle - Complete Works/aristotle-bekker-index.json | The 188-locus reverse concordance — the entry's finest-grained addressing artifact — records pdf_page values from the same superseded offset. 101 of 188 loci deviate by more than 2 pages from the entry's own validated per-work formula; the worst are ~44 pages  | **Addressed** — §5 bekker locator: reconcile the 101/188 wrong pages against archon’s observed running-head Bekker locators (draft §3.1a), not a recomputed offset; F2 (Aristotle) |
| A-56 | HIGH | corpus/index/Aristotle - Complete Works/aristotle-phys-08.json:9 | 33 of 34 unit records carry pdf_pages computed with the SUPERSEDED Phase-0 linear offset (~1.0 PDF page per Bekker page). The real compression is 1.25-2.0. Errors reach 47 pages. pdf_pages is the only machine-readable pointer from a unit into its source PDF. | **Addressed** — As A-55 — locator recalibration + check-loci round-trip; F2 (Aristotle) |
| A-57 | HIGH | corpus/index/A Grammar of Motives (Burke 1945)/_synthesis/global-edges.csv:1-877 | The canonical edge layer was never canonicalised: 876/876 rows carry resolution_status 'heuristic-pending-3A', and only 13 of the 963 distinct endpoint strings are actual ontology IDs. | **Addressed** — C5 endpoint canonicalization with quarantine counts; C1 forbids heuristic-pending status in shipped records; F2 (Grammar) |
| A-58 | CRITICAL | corpus/index/compiled-index.json crossPipelineHooks (hook-de-anima-07) | Summary/matrix sections are parsed as hooks, producing wholly empty runtime records. | **Addressed** — A3 stops the empty-hook fabrication path; hook layer superseded by C1 EDGE records at C/F (compiler frozen) |
| A-59 | LOW | corpus/index/Aristotle - Complete Works/de-anima-edges.csv:2 vs global-edges.csv:2 | The per-source and synthesis edge layers use two incompatible node-ID alphabets and are near-disjoint. The prior audit's claim that they are "the SAME data serialized twice" is REFUTED for this entry. | **Addressed** — C5 imports both Aristotle edge encodings and publishes the near-disjoint sets for resolution on the F2 checklist |
| A-60 | MEDIUM | corpus/index/Wendt - Design for Dasein/_synthesis/manifest.json:43 | Manifest status declares compiled-index registration still pending; it is not. | **Addressed** — D1 manifest gate (declared==discovered); F2 (Wendt) |
| A-61 | LOW | corpus/index/Wendt - Design for Dasein/_synthesis/dfd-terminology-appendix.md:15 | Misquotation plus wrong locus in the synthesis layer, where the unit layer has both right. | **Addressed** — C8 re-anchoring catches the synthesis-layer misquote (unit layer is right); F2 (Wendt) |
| A-62 | CRITICAL | corpus/index/Wendt - Design for Dasein/_synthesis/tension-edges.json:6-8 | All 12 tension edges are silently discarded by the compiler because the file uses a wendt_position/against schema instead of node_a/node_b. | **Addressed** — C5 TENSION schema absorbs wendt_position/against; gate requires 12/12; F2 (Wendt) |
| A-63 | MEDIUM | corpus/index/Wendt - Design for Dasein/_synthesis/quality-gates-report.md:20 | The registration-status section asserts Calleja/In-Game and Gross/Uncomfortable are absent from compiled-index.json. Both are present. | **Addressed** — D1 registration contract test (compiled name string-equals authored heading); F2 (Wendt) |
| A-64 | LOW | corpus/index/Aristotle - Complete Works/*-cross-pipeline-hooks.md -> compiled-index.json crossPipeli | Hook targetText uses ad-hoc abbreviations that match no TEXT_DIRS key and no ontologyNode text label; 'BT' and 'B&T' are the same target spelled two ways. | **Addressed** — Hook layer superseded at C (ids are the only join key per C1); F2 (Aristotle) |
| A-65 | MEDIUM | corpus/index/compiled-index.json (crossPipelineHooks, all 72 sourced from this entry) | 63 of the 72 runtime hooks have a sourceConcept that matches no ontologyNode by name, greek, or transliteration — so the entire hook array is unjoinable to the node graph. | **Addressed** — As A-64 — hooks retired in favor of resolvable EDGE records; interim damage capped by A3 |
| A-66 | MEDIUM | corpus/index/Wendt - Design for Dasein/_synthesis/book-level-ontology.md:40 | quality-gates-report.md says injecting Wendt nodes was 'Not done by hand'; a hand-written compiler shim exists and is exactly what produced the 15 runtime nodes. | **Addressed** — D1 gates.json written by checkers only (D2 separates producer from judge); F2 (Wendt) |
| A-67 | MEDIUM | corpus/index/Wendt - Design for Dasein/_synthesis/phase0-overview.md:25 | Pre-flight asserts the source PDF's OCR is clean. It is heavily corrupted. | **Addressed** — E2 admissibility probes (words-per-page, ligature, diacritic) replace narrated OCR-clean claims; F2 (Wendt) |
| A-68 | MEDIUM | corpus/index/VR Pedagogy Secondary (Part III)/_synthesis/graph-citation-network.mmd:26 | The rendered citation graph draws an arrow asserting that ped-sec-09 (Makransky & Lilleholt 2018) cites ped-sec-06 (CAMIL 2021) -- a chronological impossibility produced by rendering a cited-by edge in the cites direction. | **Addressed** — D1 unbacked-assertions-in-generated-views gate = 0; generated views only (§7 Phase 3); F2 (VR Pedagogy) |
| A-69 | MEDIUM | corpus/index/VR Pedagogy Secondary (Part III)/_synthesis/global-edges.csv:625 (and 9 sibling rows) | For the same 10 malformed rows, the synthesis layer silently TRUNCATED the note column to force 5 columns -- 10 analytical notes exist only in the per-unit files. | **Addressed** — C5 no-silent-drop import restores the 10 truncated notes from the per-unit layer; F2 (VR Pedagogy) |
| A-70 | HIGH | corpus/index/VR Pedagogy Secondary (Part III)/_synthesis/cluster-ontology.md:182 (and 18 more #### h | 19 of the entry's 60 registered nodes have their parenthetical qualifier mis-parsed into the 'greek' slot, the node name is truncated, and those junk strings are then injected into the global canonicalTerms greedy-match list. | **Addressed** — C1 TERM admission gate (§4.5: declared, length-floored, stoplisted, diff signed off) + archon-native parse retires the greek-slot mis-parse; F2 (VR Pedagogy) |
| A-71 | MEDIUM | corpus/index/VR Pedagogy Secondary (Part III)/_synthesis/graph-citation-network.mmd (51 arrows) vs 6 | The citation graph omits 16 of the 65 authored intra-cluster citation edges (25%) and adds one arrow with no CSV backing, with no note that it is a subset. | **Addressed** — D1 generated-views rule: the citation graph becomes a generated view over records, with count gate; F2 (VR Pedagogy) |
| A-72 | MEDIUM | corpus/index/VR Pedagogy Secondary (Part III)/_synthesis/global-edges.csv (1129 rows) vs per-unit -e | Two authored edges exist only in the per-unit layer and were dropped from the synthesis layer; the loss is documented as 'deduplication' but the dropped rows are not duplicates -- they carry distinct notes. | **Addressed** — §7 Phase-3 no-content-dedup rule; the 2 note-distinct rows import; F2 (VR Pedagogy) |
| A-73 | MEDIUM | corpus/index/compiled-index.json (canonicalTerms) via _synthesis/book-level-ontology.md §3A | 10 of the 15 node names that do reach runtime are unmatchable as canonical terms. | **Addressed** — §4.5 TERM gate; F2 (Wendt) |
| A-75 | HIGH | corpus/index/Von Uexkull - A Foray into the Worlds of Animals and Humans/uex-analysis/book-level-ont | Four mutually incompatible German orthographies coexist inside one entry, and the corrupted one is the one that reaches runtime. | **Addressed** — C1 normalized_quote (NFC, ligature-repaired) + E2 diacritic probe; regeneration under F fixes the runtime orthography; F2 (Uexküll) |
| A-76 | MEDIUM | corpus/index/VR Pedagogy Secondary (Part III)/Cook et al. - TES Meta-Analysis (2011)/ped-sec-15-cook | 8 unit JSONs assert in a machine-readable bridges field that the Boredom Experiment anchor entry does not exist, while it demonstrably does and 3 sibling units say so. | **Addressed** — D3 post-recompile assertion + D4 sweep retire stale cross-entry assertions; F2 (VR Pedagogy) |
| A-77 | LOW | corpus/index/Red Dead Redemption 2 Secondary (2019-2023)/_synthesis/cluster-ontology.md:1 (and 9 sib | A keyboard-mash template artifact '# dfd… —' is baked into the H1 of 10 of the 11 _synthesis markdown files. | **Addressed** — F2 checklist (RDR2 Secondary): template artifact removed at regeneration |
| A-78 | HIGH | corpus/index/VR Pedagogy Secondary (Part III)/Makransky & Petersen - Desktop VR SEM (2019)/ped-sec-1 | 10 per-unit edge rows contain unquoted commas inside the target field, producing 6- or 7-column records against a declared 5-column header. | **Addressed** — C1 canonical CSV format with quoting rules, gated on exact header + parse; F2 (VR Pedagogy) |
| A-79 | HIGH | corpus/index/Red Dead Redemption 2 Secondary (2019-2023)/_synthesis/cluster-ontology.md:1-40 | Registration alone would NOT fix this entry: the ontology file yields zero nodes under both compiler parsers. | **Addressed** — F2 regenerates RDR2 Secondary under the locked system (registration alone is insufficient — confirmed); C5 migrates its relational content |
| A-81 | HIGH | corpus/index/Virtual Learning Environments (King–Salvo)/ (all 9 files) | The entire entry is untracked by git and not gitignored — no commit contains it. | **Addressed** — P0.2 commits the VLE tree (done at Phase 0) |
| A-82 | MEDIUM | corpus/index/Red Dead Redemption 2 Secondary (2019-2023)/_synthesis/cluster-ontology.json (concept_n | RDR2 has no concept IDs anywhere; the ontology and the concept matrix are joined only by free-text labels that have drifted, so 13 of 21 ontology concepts and 16 of 24 matrix rows have no counterpart. | **Addressed** — C1 ids everywhere; C5 label-drift quantified; F2 (RDR2 Secondary) |
| A-83 | CRITICAL | corpus/index/Virtual Learning Environments (King–Salvo)/_synthesis/book-level-ontology.md:14,35,42,4 | 7 of the 18 nodes that ARE the entry's entire runtime surface carry counts that the entry's own unit layer and the authoritative analysis file explicitly superseded on 2026-07-23/24. Nothing in the entry records that the ontology is the stale layer. | **Deferred** — Deferred — the VLE stale-node set is queued in the user’s staleness-repair session (archive 13-BOOT waves; DO-NOT-TOUCH #3). D3/D4 prevent recurrence; F2 verifies the repair landed |
| A-84 | CRITICAL | scripts/compile-corpus-index.py:35-133 (TEXT_DIRS) | The entry is absent from TEXT_DIRS, so the compiler never opens any of its 60 files. | **Addressed** — A7 + C imports + D1 check-registration; F2 (RDR2 Secondary) |
| A-85 | MEDIUM | corpus/index/Red Dead Redemption 2 Secondary (2019-2023)/*/rdr-*-edges.csv (source column) | All 12 per-unit edge files are star graphs: every row's source is the unit itself. There are zero concept-to-concept edges in the entire entry. | **Addressed** — F2 regeneration under D1 distribution/edge gates (star graphs cannot pass); C5 preserves what exists |
| A-86 | HIGH | corpus/index/Presence Theory (Foundations)/_synthesis/bridge-to-part-ii.md:19 and :55 vs Lepecq et a | The bridge presents three Lepecq quotations as FAITHFUL-LEPECQ 'verified quotes' on pages 147/148-149/150 that appear in no verification record, and two of them are rendered in wording that differs from the same phrases in the unit file — so at least one rende | **Addressed** — C8 re-anchors the three FAITHFUL-LEPECQ quotations via match_kind gate with B4 classes; F2 (Presence Theory) |
| A-87 | HIGH | corpus/index/Virtual Learning Environments (King–Salvo)/_synthesis/book-level-ontology.md:70 | Node 9's projected reading is an interpretation the project formally retracted on 2026-07-13 and refined again on 2026-07-23, yet it is what reaches runtime. | **Deferred** — Deferred — same repair queue as A-83 (retracted interpretation is a content-repair item in the user’s session); D3 blocks it from surviving future recompiles; F2 verifies |
| A-88 | LOW | corpus/index/Presence Theory (Foundations)/ (all 10 files) | The entire entry is untracked by git and not gitignored. | **Addressed** — P0.2 commits the Presence Theory tree (done at Phase 0) |
| A-89 | MEDIUM | corpus/index/Presence Theory (Foundations)/ (entry-wide) | Zero machine-readable source paths. All four source PDFs exist on disk, but nothing in the entry records where. | **Addressed** — C1 SOURCE records (sha256, path, document_id per C6); F2 (Presence Theory) |
| A-90 | LOW | corpus/index/Red Dead Redemption 2 Secondary (2019-2023)/_synthesis/concept-matrix.csv | Three concept-matrix rows name concepts that exist in no ontology node. | **Addressed** — C1 resolvable-endpoint rule; F2 (RDR2 Secondary) |
| A-91 | MEDIUM | corpus/index/VR Pedagogy Secondary (Part III)/Chow - Presence Determinants (2016)/ped-sec-03-chow-pr | The shifts-debate-axis relation has no controlled vocabulary: all 40 rows target free-text axis prose instead of the DA-NN ids, 21 distinct spellings encode 10 axes, and 2 rows target a unit id instead of an axis. | **Addressed** — As A-13 — DA-NN registry ids; F2 (VR Pedagogy) |
| A-92 | MEDIUM | scripts/compile-corpus-index.py:735-738 → corpus/index/compiled-index.json | The compiler truncates node names at a trailing parenthetical and files the parenthetical into the `greek` (Greek-lemma) field, then injects that English fragment into canonicalTerms as a matchable term. 4 of the 18 VLE nodes are affected. | **Addressed** — As A-70 — compiler frozen; archon-native TERM parse + §4.5 gate supersede; F2 (VLE) |
| A-93 | MEDIUM | corpus/index/Red Dead Redemption 2 Secondary (2019-2023)/*/rdr-*.json (concepts field) | 9 of 12 unit records store `concepts` as bare strings; only 3 store objects. | **Addressed** — C1 CLAIM/concept schema (objects, not bare strings) at import and regeneration; F2 (RDR2 Secondary) |

### Refuted

| ID | Claim as filed | Why it was refuted | Refutation record |
|---|---|---|---|
| A-28 | The file's declared gaps_per_section contradicts the gaps array it ships, and both contradict the per-section citation-gap-table.json files. | Records-vs-gaps denominator error; the batch entries expand to exactly 207; only a narrow off-by-one survives | Adversarial review §6.3; verification record in raw/workflow-journals/A-entry-audit.journal.jsonl |
| A-39 | Four mutually non-joinable identifier namespaces for the same concept set; 0 of 45 matrix rows join to the ontology node ids. | Headline number reproducible but the join target and stated consequence are wrong (severity collapse) | Adversarial review §6.3; verification record in raw/workflow-journals/A-entry-audit.journal.jsonl |
| A-74 | manifest declares synthesis_files: 22; the _synthesis directory contains 24 files. | Raw ls count vs curated-manifest denominator; a design decision mistaken for a defect | Adversarial review §6.3; verification record in raw/workflow-journals/A-entry-audit.journal.jsonl |
| A-80 | vle-05's sole declared source path is unresolvable in two independent ways: the directory case is wrong AND the PDF does not exist anywhere in the repository. | The PDF exists at the exact declared relative path under the archon corpus root; both central assertions and the consequence false | Adversarial review §6.3; verification record in raw/workflow-journals/A-entry-audit.journal.jsonl |

## Workflow B — every line of index code

### Confirmed

| ID | Sev | Location / scope | Finding (as filed) | Disposition |
|---|---|---|---|---|
| B-01 | CRITICAL | scripts/compile-corpus-index.py:1110-1111 and :1128-1129 | 55 of the 119 reachable tension records are discarded with no warning, no counter, and no exit code, because _normalize_tension_edge returns None for any schema that does not literally use node_a/nodeA + node_b/nodeB, and parse_tension_edges drops None silentl | **Addressed** — C5 TENSION schema absorbs all eight shapes with zero-silent-drop gate (55/119 loss becomes impossible); compiler frozen per settled decision |
| B-02 | CRITICAL | scripts/compile-corpus-index.py:1318-1348 | deduplicate_ontology_nodes merges nodes across DIFFERENT books on lowercase name alone and then keeps fields from different books in the same record, fabricating composite concept entries that correspond to no source. | **Addressed** — Superseded at C1: records join by id, merges by (name,text) never name-alone; the frozen compiler’s merge is retired with it at F |
| B-03 | LOW | scripts/compile-corpus-index.py:1375 | builtAt is stamped from the wall clock unconditionally, so no two builds can ever be byte-identical and the git-tracked artifact always shows a diff. | **Deferred** — Deferred — frozen legacy compiler; builtAt nondeterminism is tolerated in the interim and consumed constructively by A8’s staleness check; retired at F |
| B-04 | MEDIUM | scripts/compile-corpus-index.py:1067-1070 | The Metaphysics arm of _infer_target_text is dead code and every metaphysics-targeted hook is mislabelled 'Physics', because 'metaphysics' contains 'physics' and the physics test runs first. | **Deferred** — Deferred — dead arm in a frozen compiler; hook layer superseded at C; retired at F |
| B-05 | HIGH | scripts/explore/verify/immutability_checker.py:402-443 | `--verify --json` always exits 0, even when violations are present. The `return 1` exists only inside the human-readable branch. | **Deferred** — Deferred — legacy QA tooling; D1 checkers (exit non-zero by construction) replace it for the new system |
| B-06 | HIGH | scripts/compile-corpus-index.py:1239-1244 | Cross-pipeline hooks can only ever come from Aristotle: the collector hardcodes one directory literal and globs only inside it. | **Addressed** — Hook layer superseded at C1 (EDGE records from all entries, not one hardcoded directory); interim capped by A3 |
| B-07 | LOW | .github/workflows/god-audit.yml:31 + scripts/audit/core/missing_link_detector.py:27-36 | The four audit scripts the workflow invokes directly cannot be executed without PYTHONPATH=scripts. That requirement exists in exactly one place — a workflow env block — and the scripts' own sys.path insertion points at the wrong directory. | **Deferred** — Deferred — legacy CI plumbing for scripts the new system does not use |
| B-08 | CRITICAL | scripts/compile-corpus-index.py:1185-1188 | TEXT_DIRS is a hardcoded allow-list of 19 directory names. Eight real entry directories exist under corpus/index/ and are silently excluded from every compile. There is a warning for a listed-but-missing directory and no check at all for an unlisted-but-presen | **Addressed** — A7 transfer + registration-by-glob (D1 check-registration) + C imports read from disk; TEXT_DIRS retired |
| B-09 | MEDIUM | scripts/compile-corpus-index.py:1283 and :1304 | canonicalTerms is emitted from a Python set with a sort key that ties for every case-variant pair, so 202 of 1,722 term positions are ordered by PYTHONHASHSEED-dependent set iteration. | **Deferred** — Deferred — frozen compiler nondeterminism; C1 TERM registry (sorted, versioned) supersedes canonicalTerms at C/F |
| B-10 | CRITICAL | .github/workflows/god-audit.yml:4-21, .github/workflows/god-learn-qa.yml:3-21 + git tracking state | Both workflows depend on files that are not in git. A fresh CI checkout cannot satisfy them, so neither pipeline can ever have been green. | **Addressed** — P0.2/P0.3/P0.4 put the untracked dependencies in git (the immediate defect); CI strategy itself is open question N2 in 03-OPEN-QUESTIONS |
| B-11 | MEDIUM | scripts/explore/verify/immutability_checker.py:247-327 (verified against .god-verify/immutability_ba | The immutability gate is currently failing with 17 violations and has been since the baseline was written on 2026-01-13. Nothing reports this because god-learn-qa.yml's path filter does not include corpus/** or scripts/ingest/**. | **Deferred** — Deferred — legacy immutability gate; D1 gate suite replaces it; no new work on the old checker |
| B-12 | CRITICAL | scripts/compile-corpus-index.py:14-15, :153-154 | The module docstring promises stderr warnings for every skipped row; on the current corpus the compiler emits ZERO warn() calls while discarding content at 20+ distinct in-range sites. | **Addressed** — Superseded: D1 validators exit non-zero and count discards (the compiler is frozen and never becomes the record producer) |
| B-13 | MEDIUM | scripts/compile-corpus-index.py:1050-1071, :883, :1019 | targetText is drawn from two incompatible vocabularies — _infer_target_text's nine hardcoded short names and raw parenthetical text scraped from hook titles — and not one of the ten distinct values emitted joins to any text label the compiler itself produces. | **Addressed** — Hook layer superseded at C1; interim capped by A3 |
| B-15 | HIGH | scripts/compile-corpus-index.py:308-321, :324-347 | _parse_field_value_ontology splits the ENTIRE file on `### N` headers with no section restriction, so the last concept section absorbs every remaining byte of the file — and 96.8% of that absorbed content is then silently discarded. | **Addressed** — Superseded at C/F: archon-native records are produced from structured sources, not the markdown scrape; the frozen compiler’s parser loss ends at F |
| B-16 | CRITICAL | scripts/compile-corpus-index.py:216-217 | A single line matching `∣ Field ∣ Value ∣` anywhere in a table-format file reroutes the ENTIRE file to the field/value parser, discarding every row of the real 3A canonical node table with no warning. | **Addressed** — As B-15 (parser rerouting defect in the frozen compiler) |
| B-17 | HIGH | scripts/compile-corpus-index.py:260-261 | Any markdown table row with fewer than 5 pipe-cells is discarded with no warning, no counter, and no trace. This is destroying real claim rows in the live corpus today. | **Addressed** — As B-15; C5 imports the discarded claim rows directly from source files |
| B-18 | MEDIUM | scripts/audit/core/provenance_tracer.py:350-368 and :318-348 | _resolve_pdf_path falls back to matching by BASENAME inside three guessed subdirectories. When that fallback fires, the tracer hashes a different file than the one the record names and marks the provenance node verified=True. | **Deferred** — Deferred — legacy audit tooling (provenance_tracer); C1 SOURCE sha256 + C6 document_id make the guessing fallback unnecessary in the new system |
| B-19 | HIGH | scripts/compile-corpus-index.py:379-390, :495-506 | The emitted node schema carries NO provenance whatsoever — no source file, no line number, no section anchor, no unit id, no stable node id. Nothing this compiler produces can be traced back to a document, let alone a clause. | **Addressed** — C1 IS the fix: ids, spans, provenance and anchors are the schema (draft §3), replacing the provenance-free emitted node schema |
| B-20 | MEDIUM | scripts/compile-corpus-index.py:409-427 | The header map is built ONLY when the data row's cell count exactly equals the header row's. One ragged row silently switches to positional parsing mid-table, reading the definition column as units and emitting an empty definition. | **Addressed** — As B-15 (ragged-row positional parsing in the frozen compiler); C1 gated CSV format at C5 import |
| B-21 | HIGH | scripts/compile-corpus-index.py:350-358 | The concept title is split on the FIRST dash-class character with a non-greedy group, so any hyphenated word in a title corrupts the node name. This has already shipped in compiled-index.json. | **Addressed** — As B-15 (dash-split name corruption); C1 TERM/наme rules; F2 fixes shipped instances |
| B-22 | HIGH | src/god-agent/retrieval/faceted-retrieval.ts:154-164 and :186-195 | Both hybridSearch call sites pass FOUR arguments to a THREE-parameter method. The fourth argument — which carries maxChunks, minRelevance, collections and diversityBoost — is discarded at runtime. tsc reports both, but nothing runs tsc. | **Deferred** — Deferred — incumbent retrieval quality; interim errors accepted by directive; retired when archon drafting takes over (Phase F). Not wraith-specific |
| B-23 | HIGH | src/god-agent/retrieval/smart-retrieval-layer.ts:131-176, :303, :210 | A transient ChromaDB failure at first use permanently disables retrieval for the whole process AND poisons the cache with empty results for 30 minutes — while retrieveContext's own comment claims it throws on infrastructure errors. | **Deferred** — Deferred — incumbent resilience defect; interim accepted; archon evidence path (B1/C) replaces this retrieval stack |
| B-24 | HIGH | src/god-agent/retrieval/smart-retrieval-layer.ts:194 (only read site) ↔ writers at retrieval-stage.t | RetrievalOptions.collections is normalized once at :194 and then read by nothing. It never reaches the embedding call, the Chroma query body, or the where filter. Every caller that believes it is scoping retrieval is getting an unscoped whole-corpus search. | **Deferred** — Deferred — as B-23 (dead collections option) |
| B-25 | HIGH | scripts/compile-corpus-index.py:718-721 | The terminator arm `elif stripped.startswith("###") or stripped.startswith("---"): break` is dead the instant any field has been seen, because the preceding arm `elif current_key and stripped:` matches every non-empty line and `current_key` is never reset to f | **Addressed** — A2 reorders the arms so the terminator precedes the absorb arm (the dead-break mechanism itself) |
| B-26 | CRITICAL | src/god-agent/universal/stages/retrieval-stage.ts:226 (also :303, :343; src/god-agent/universal/stag | The chunk de-duplication key reads two metadata fields that the ingest pipeline never writes, so every chunk collapses to the string "undefined:<page_start>" and de-duplication silently becomes "one chunk per page number, corpus-wide." | **Addressed** — A12: dedupe on chunk.chunkId at the three retrieval-stage sites + 16 orchestrator sites |
| B-27 | HIGH | src/god-agent/retrieval/smart-retrieval-layer.ts:252-291 (boost) applied after :239-241 (rerank) | The +0.10 canonical-term boost is additive on top of the cross-encoder score and clamped to 1.0, so it saturates the top of the distribution and lets low-ranked chunks jump above the reranker's best hits. The project's own evaluation concluded the reranker is  | **Deferred** — Deferred — boost-saturation redesign belongs to the archon retrieval path; A5/A6 remove the reranker interplay; interim accepted. (Shared-code finding, kept in scope, not wraith-abandoned) |
| B-28 | CRITICAL | src/god-agent/universal/stages/retrieval-stage.ts:318-320 → src/god-agent/core/composition/retrieval | trimChunkContent does not truncate — it scores sentences, takes the highest-scoring ones up to 450 chars, restores document order, and joins them with a single space. The result is a NON-CONTIGUOUS collage of sentences with no elision marker, handed to the LLM | **Deferred** — Deferred — incumbent collage-trim; C1 principle (immutable sourceText; trimmedContent separate) governs the archon path; interim accepted |
| B-29 | HIGH | scripts/compile-corpus-index.py:35-133 (TEXT_DIRS) driving :1185 and :1258 | The set of compiled entries is a hand-maintained literal dict. Eight real entry directories exist on disk and are never compiled; three hold a cluster-ontology.md the existing header parser would read unchanged. | **Addressed** — As B-08 (TEXT_DIRS literal retired by registration-by-glob + transfer) |
| B-30 | CRITICAL | src/god-agent/retrieval/smart-retrieval-layer.ts:303, :305, :1430 ↔ src/god-agent/universal/stages/r | retrieveContext caches the exact ContextChunk object references it returns; retrieval-stage then mutates those objects in place. The mutations write straight through into the 30-minute LRU cache, permanently poisoning it with trimmed collages and fabricated sc | **Deferred** — Deferred — incumbent cache mutation; C1 immutability principle in archon path; interim accepted |
| B-31 | HIGH | scripts/compile-corpus-index.py:538-540 | Everything before the first `#### N. ` / `**N. ` marker is discarded without inspection or warning. Measured: 181,140 of 482,731 bytes (37.5%) of header-format ontology source never enters the parse loop. | **Addressed** — As B-15 — the 37.5% preamble discard ends when records come from structured sources (C5/F); frozen compiler unpatched by design |
| B-32 | MEDIUM | scripts/compile-corpus-index.py:718-719 -> corpus/index/compiled-index.json node name="Temporalität" | A second shipped node is polluted by the same mechanism: centralityTier is 247 chars of Section 3C tension prose and units is 586 tokens of the Section 3E book-trajectory narrative. The prior audit reported only one polluted node. | **Addressed** — A2 fixes the mechanism (Temporalität node); acceptance asserts clean recompile |
| B-33 | MEDIUM | src/god-agent/universal/gold-standard-prompt-builder.ts:725-727 (identical at :1088-1090) | The duplicate-bridge filter can never match. hookLines render `[sourceText → targetText]`, never the hook id, so `line.includes('['+mandatoryBridgeId+']')` is always false and the mandatory bridge is injected TWICE whenever it also scores top-3. | **Addressed** — A3 removes the fabricated-bridge emission this filter failed to guard; hook rendering superseded at C |
| B-34 | MEDIUM | src/god-agent/universal/cli.ts:1650-1656 + src/god-agent/universal/universal-agent.ts:2247-2255 | Without --execute, every corpus and index flag is parsed, validated, and then silently discarded. prepareWriteTask() builds the drafting prompt from writing instructions + topic only — zero chunks, zero ontology nodes, zero hooks, zero tensions, zero knowledge | **Deferred** — Deferred — incumbent CLI flag handling; interim accepted; drafting moves to archon at F |
| B-35 | HIGH | src/god-agent/universal/write-pipeline-orchestrator.ts:3791; .claude/commands/god-write.md:110-116 | The documented "auto-revise up to 3 times to reach 0.85" never happens. The CLI leaves maxGauntletRevisions undefined and writeV2 coerces it to 0, so validateAndRevise scores once and returns. | **Deferred** — Deferred — incumbent gauntlet revision loop; interim accepted; FCDP/archon drafting supersedes |
| B-36 | CRITICAL | src/god-agent/universal/gold-standard-prompt-builder.ts:704-718; src/god-agent/shared/cross-author-u | 31 of 72 hooks have an empty targetConcept. `t.includes(targetLower)` with targetLower === '' is always true, so those 31 match EVERY topic. getActiveBridges therefore never returns empty, and the prompt emits a MANDATORY block naming author "Unknown", concept | **Addressed** — A3 (empty-targetConcept guard + Unknown-author refusal) |
| B-37 | MEDIUM | scripts/compile-corpus-index.py:735-736 and :758 | Any parenthetical in a header-section title is promoted to the node's `greek` field, and build_canonical_terms (:1286) then emits it as a canonical matching term. 259 of the 448 nodes carrying a greek value contain no Greek characters at all. | **Addressed** — As A-70/A-92 — §4.5 TERM admission gate supersedes; compiler frozen; F2 fixes shipped instances |
| B-39 | HIGH | scripts/compile-corpus-index.py:718-719 -> corpus/index/compiled-index.json node name="Preparatory R | The shipped compiled index contains a node whose centralityTier is a 10,009-character dump of Rickert's entire Section 3B global edge list, and whose units array is 973 whitespace-split garbage tokens derived from an equally polluted `chapters` field. | **Addressed** — A2 (the 10,009-char Preparatory Rhetoric node); acceptance asserts clean recompile |
| B-40 | HIGH | src/god-agent/universal/write-pipeline-orchestrator.ts:1156; :537-557; src/god-agent/universal/stage | sectionConstraints is a flat list holding 0–3 entries per section, but writeRollingContext indexes it positionally as one-per-section. Constraints are attached to the wrong sections. | **Deferred** — Deferred — incumbent orchestrator constraint indexing; interim accepted; retired at F |
| B-42 | HIGH | scripts/ingest/parallel_ingest.py:589-592, :659-662 | parallel_ingest.py catches and logs the ChromaDB upsert exception, then _flush_batch unconditionally reports the whole batch as written. A store that rejects every write yields "64 chunks written", a clean summary, and exit code 0. | **Addressed** — E4: batch write failures must fail the batch report (named test in archon ingest suite) |
| B-43 | MEDIUM | src/god-agent/universal/gold-standard-prompt-builder.ts:125-130 vs :136-159, :636 | The Source Index is built from ALL distinct works before truncation, but the chunk body is cut at 60,000 chars. The prompt then tells the model that any author in the Source Index is citable — including works whose text was never shown. | **Deferred** — Deferred — incumbent prompt-builder truncation asymmetry; interim accepted; archon pack builder supersedes |
| B-44 | HIGH | scripts/ingest/parallel_ingest.py:187-188, :196 | parallel_ingest.py's skip check compares the first 12 hex chars of a first-64KB hash against the first 12 hex chars of a stored FULL-file hash. For any file larger than 64 KB these are different hash functions and can never agree, so the skip path is dead and  | **Addressed** — E4: like-for-like hash comparison requirement (named test) |
| B-45 | HIGH | scripts/ingest/parallel_ingest.py:345-352 | parallel_ingest.py attaches bounding boxes to chunks by POSITIONAL index into a differently-segmented list. `chunks` comes from re-chunking `bbox_chunks`'s text through chunk_paragraphs_page_aware, so index i in one list is not the same span as index i in the  | **Addressed** — E4: keyed (not positional) bbox attachment requirement (named test) |
| B-46 | HIGH | src/god-agent/universal/corpus-index-provider.ts:81-91, :108, :118, :135, :150 | Index relevance is raw substring frequency over ≥4-char whitespace tokens with no stopword filter, no length normalization, and no minimum score. Any topic — including one with zero conceptual overlap — retrieves a full complement of nodes, hooks, and tensions | **Addressed** — Superseded by archon’s index reader via A8/C (canonical expansion, owning-work routing); incumbent substring scorer retired at F |
| B-47 | HIGH | scripts/ingest/run_ingest_phase2.py:1687, :1830; scripts/ingest/reingest_layout.py:169-173 | The only chunk-reconciliation key in the entire ingest tree is exact `path_rel` string equality. There is no purge tool, no orphan sweep, and no delete keyed on anything else. A renamed or moved file orphans all its old vectors permanently, and so does a chang | **Addressed** — E4: rename-reconciliation requirement (named test) |
| B-50 | HIGH | scripts/ingest/run_ingest.py:395-408, :445-461 | run_ingest.py writes manifest records with status="ok" and a chunk count while producing ZERO embeddings and ZERO Chroma writes. 531 of the 696 latest manifest records (76%) are of exactly this shape, and every one of them has no vectors in the store. | **Addressed** — E4: status="ok" requires embeddings written (named test) |
| B-51 | HIGH | scripts/normalize-edges.py:35-42, :239-247 | KNOWN_PIPELINES is a six-entry hardcoded registry; `--all` (the only batch mode) reaches 6 files and 792 canonical edges out of the index's 240 CSVs / 20,610 edge rows — 3.8% coverage. | **Addressed** — Superseded: C5 imports the 240 CSVs directly; normalize-edges.py (zero callers, untracked) is never promoted |
| B-52 | HIGH | scripts/ingest/run_ingest_phase2.py:1030, :1069-1081 | The Marker pre-fetch holds every PDF's full Marker JSON block tree for the entire corpus in one dict, resident until the process exits. There is no eviction, no cap, and no streaming. | **Deferred** — Deferred — legacy ingest memory behavior; archon ingest + E carries memory discipline; the incumbent script is not on the post-F path |
| B-53 | CRITICAL | scripts/reason/reason_over_knowledge.py:325-338 and :396-409 vs src/god-agent/retrieval/types.ts:58- | Phase 7 emits reasoning rows with knowledge_ids and evidence but NO source/target. The runtime zod schema requires both as non-optional strings, so every record it writes is dropped at load. The live reasoning graph is empty at runtime. | **Addressed** — C7: reasoning rows map into the C1 EDGE model at import (endpoints from knowledge_ids, quarantine counted); no incumbent patch |
| B-55 | HIGH | scripts/reason/reason_over_knowledge.py:449-457 invoked from scripts/god_learn/god_learn.py:637-645 | write_outputs truncates god-reason/reasoning.jsonl on every Phase 7 run, destroying whatever merge-reasoning-edges.py anchored there. Phase 7 runs automatically inside `god-learn update` unless --skip-reasoning is passed. | **Addressed** — A10: truncation guard + timestamped .bak before write_outputs clobbers reasoning.jsonl |
| B-57 | CRITICAL | scripts/merge-reasoning-edges.py:19 and :22 with :206 | PHASE7_SOURCE and OUTPUT are the same path. The script truncates its own input with open(OUTPUT, 'w') and takes no backup. | **Addressed** — A10: same guard on merge-reasoning-edges (PHASE7_SOURCE==OUTPUT self-truncation) |
| B-58 | CRITICAL | src/god-agent/shared/cross-author-utils.ts:474 | `t.includes(targetLower)` with an empty `targetConcept` is ALWAYS true in JavaScript, so all 31 hooks that have an empty targetConcept fire for EVERY facet, unconditionally. | **Addressed** — A3 (the always-true includes guard) |
| B-59 | CRITICAL | src/god-agent/shared/cross-author-utils.ts:56, :342, :351 | `_hooksDerivationDone` is a module-global one-shot with NO reset anywhere in the repository, while the object it guards is replaced by a fresh parse on every mtime change. | **Addressed** — A3 (delete the _hooksDerivationDone one-shot) |
| B-60 | HIGH | src/god-agent/shared/cross-author-utils.ts:317 | `buildCorpusCatalog` defaults every title with a missing authority_tier to 'secondary', mislabelling 71 of 117 works — including Heidegger's *The Fundamental Concepts of Metaphysics* and the Greek critical editions of De Anima, Physics and Rhetoric — as SECOND | **Addressed** — A13 (authority_tier → UNCLASSIFIED, never secondary) |
| B-61 | CRITICAL | src/god-agent/shared/cross-author-utils.ts:498 -> src/god-agent/universal/gold-standard-prompt-build | The single bridge selected for injection is, for most topics, hook-de-anima-07 — a record whose targetText, targetConcept, bridge and relevance are ALL empty strings — and the prompt instructs the model that it MUST cite an author literally named 'Unknown'. | **Addressed** — A3 (Unknown-author bridge refusal removes the hook-de-anima-07 all-empty-fields injection); hook layer superseded at C |
| B-62 | HIGH | src/god-agent/shared/cross-author-utils.ts:159-167 -> :307-331 | `authorTitles` is only populated when BOTH author_raw and title_raw exist, so the catalog headed 'only suggest from this list' lists 117 of the 578 titles in the corpus — it declares 80% of the ingested corpus non-existent. | **Deferred** — Deferred — incumbent catalog coverage (117/578); interim accepted; C1 SOURCE records supersede the catalog at F |
| B-63 | CRITICAL | src/god-agent/shared/jsonl-loaders.ts:202, :215-217 | The ONLY reader of compiled-index.json wraps everything in a bare `catch {}` that returns null with no log, no counter, and no distinction between 'file absent', 'permission denied' and 'JSON is corrupt'. | **Deferred** — Deferred on the incumbent loader (interim accepted); the archon-side equivalent is addressed by A8 (loud wrong-path) + A9 (logged .ok() swallows) |

### Refuted

| ID | Claim as filed | Why it was refuted | Refutation record |
|---|---|---|---|
| B-14 | corpus/index and compiled-index.json are absent from the protected list. The artifact the whole retrieval stack depends on has zero integrity protection, while 1.5 GB of PDFs are hashed on every run. | Code quote accurate, but the filed consequence/remedy fail — the proposed protected-list gate would be permanently red (builtAt stamps every run); arithmetic off ~35× | Adversarial review §6.2; raw/workflow-journals/B-code-audit.journal.jsonl |
| B-38 | _parse_header_tables only looks inside a section whose heading matches `^##\s+3A[.:]` and returns an empty list otherwise with no warning. 153 of the 190 pipe-table rows in header-format ontologies li | The 3A window is deliberate and protective; simulating the “fix” fabricates 62 nodes named depends_on/presupposes/… | Adversarial review §6.2; raw/workflow-journals/B-code-audit.journal.jsonl |
| B-41 | run_ingest.py's skip predicate checks only sha256+status and ignores `phase` entirely, so the 531 phase-less no-vector records are permanently self-perpetuating: re-running Phase 1 skips them forever  | Figures accurate; the proposed fix is actively harmful (would re-append all 531 records every run) | Adversarial review §6.2; raw/workflow-journals/B-code-audit.journal.jsonl |
| B-48 | normalize_row emits a fully-formed canonical edge even when source, relation and target are all empty, and stamps a fabricated confidence='medium' on it. 8,889 of the index's 20,610 CSV rows (43.1%) b | Wrong metric (real blank triples: 895 = 4.3%, not 8,889); unreachable through the CLI (exit 2); script has zero callers and is untracked | Adversarial review §6.2; raw/workflow-journals/B-code-audit.journal.jsonl |
| B-49 | The script is not idempotent. A second run reads its own merged output as 'Phase 7 edges' and unconditionally re-tags every manual and LLM edge as derivation='phase7', generation_epoch=1. | Refuted as filed (merge reaches a fixpoint; hand-curated boundary preserved) — BUT a second run truncates unanchored-edges.jsonl 1,141→0: that surviving finding is carried into step A10 | Adversarial review §6.2; raw/workflow-journals/B-code-audit.journal.jsonl |
| B-54 | cmd_verify calls deduplicate_manifest() as its FIRST action, rewriting scripts/ingest/manifest.jsonl in place. Measured: 1,182 -> 696 records, destroying 250 corpus/index rows including ALL 248 status | cmd_verify dedup is safe: all 248 removed rows have a strictly later ok row for the same path; logged; git-tracked. The audit’s most confidently-wrong prior item | Adversarial review §6.2; raw/workflow-journals/B-code-audit.journal.jsonl |
| B-56 | `clearJSONLCaches()` nulls `_compiledIndexCache` but has no knowledge of `_hooksDerivationDone` in the other module, so calling it deterministically triggers the undefined-author bug — it is a loaded  | Project is ESM — the require throws before the claimed path executes. Bonus real finding recorded: quality-integration.ts:749 swallows the same broken require, leaving ontology expansion permanently empty | Adversarial review §6.2; raw/workflow-journals/B-code-audit.journal.jsonl |

## Workflow C — entry-creation plans

### Confirmed

| ID | Sev | Location / scope | Finding (as filed) | Disposition |
|---|---|---|---|---|
| C-02 | HIGH | plans/secondary-cluster-index-plan-TEMPLATE-2026-07-15.md:115 and :173 | 'global-edges.csv — all per-unit edges concatenated + deduped' and '3B global-edges.csv (concat + dedup)' specify a deduplication with no key. locus and note are the evidence-bearing columns; whether they participate in the key decides whether evidence survive | **Addressed** — §7 Phase-3: aggregation is concatenation keyed on edge_id; near-duplicate detection is a human-resolved report; D1 gate count(global)==Σcount(per-unit) |
| C-03 | CRITICAL | plans/boredom-secondary-cluster-index-plan-2026-07-14.md:165-167 | The section 4 controlled vocabulary controls object types and edge relations but not FIELD VALUES. There is no enum for stance, support tier, treatment, role, or reuse disposition anywhere in the 287 lines. The provenance tag vocabulary at lines 31-35 (FE / FA | **Addressed** — C1/§4: closed enums for stance, support_tier, evidence_type, provenance, disposition — validated, null illegal |
| C-04 | CRITICAL | plans/secondary-cluster-index-plan-TEMPLATE-2026-07-15.md:162 | '-edges.csv — columns source,relation,target,locus,note' is the only column declaration in the plan, it is stated once for the per-unit file, it is never restated for the aggregated global-edges.csv, and it contains no edge id, no source/target type discrimina | **Addressed** — C1: one canonical EDGE format stated verbatim, gated on exact header equality at both levels |
| C-05 | CRITICAL | plans/boredom-secondary-cluster-index-plan-2026-07-14.md:101-106 | The plan specifies an id scheme for exactly one level — the unit (bor-sec-NN, zero-padded, slug = first-author surname). It specifies no id scheme, no prefix rule and no uniqueness scope for anything BELOW the unit. It also does not forbid agents from inventin | **Addressed** — C1: global id namespaces with registry reservation at Phase-1 registration (§7); D1 check-ids cross-entry |
| C-06 | CRITICAL | plans/boredom-secondary-cluster-index-plan-2026-07-14.md:203-205 | The unit JSON is specified as a bag of parallel arrays. There is no record type in the entire plan — not in the section 4 controlled vocabulary (lines 165-167), not in the Phase 2 spec — that is defined as an addressable object with a stable id. The atom of th | **Addressed** — C1 CLAIM/CLAUSE record model IS the remedy for the parallel-array unit bag (the plan’s central reframing) |
| C-07 | CRITICAL | plans/secondary-cluster-index-plan-TEMPLATE-2026-07-15.md:107-133 and :187-197 | The mandated folder layout specifies ~14 _synthesis artifacts plus three files per source, and the Phase-5 wiring specifies exactly one TEXT_DIRS dict entry with ontology_format 'header' and analysis_subdir '_synthesis'. The compiler reads, from that configura | **Addressed** — §11 runtime contract (reader table pasted into gates report) + D1 check-reachability ≥0.95 + per-layer non-zero gate |
| C-08 | HIGH | plans/secondary-cluster-index-plan-TEMPLATE-2026-07-15.md:109-133 | The mandated folder layout omits `tension-edges.json` entirely. It mandates `debate-map.{md,json}` (:116) instead. But `scripts/compile-corpus-index.py:1267` reads ONLY `<analysis_subdir>/tension-edges.json` when collecting tension edges; it never opens debate | **Addressed** — C5 imports tensions regardless of filename; D1 reachability gate; the count-only/no-key-name defect class is closed by schemas-not-counts (C1) |
| C-09 | HIGH | plans/boredom-secondary-cluster-index-plan-2026-07-14.md:170-171 | The bridge relations are defined by naming the relation and describing the target in prose: 'grounds-measure -> a Boredom Experiment dataset channel; bridges-to-part-iii -> the dissertation'. No target id space is enumerated. Line 252-253 then instructs the or | **Addressed** — C1 EDGE endpoints registry-resolved; prose targets illegal |
| C-10 | CRITICAL | plans/boredom-secondary-cluster-index-plan-2026-07-14.md:204 | The unit JSON spec names seven collection keys (sections, constructs, measures, readings, concepts, interlocutors, tensions) and gives a per-field schema for exactly one of them — measures, at line 205 ('populate measures[] richly (signal, apparatus, sample N, | **Addressed** — C1 JSON Schema files per record type with required fields/types (one worked exemplar each) |
| C-11 | CRITICAL | plans/boredom-secondary-cluster-index-plan-2026-07-14.md:228-243 | Section 6 wires the entry into the compiler by adding one TEXT_DIRS entry with ontology_format 'header'. The plan assumes this makes the entry visible to the runtime. It does not. scripts/compile-corpus-index.py reads exactly two files per registered entry: th | **Addressed** — §11 runtime contract + D1 per-layer non-zero contribution gate (registration ≠ reachability, made explicit) |
| C-12 | CRITICAL | plans/secondary-cluster-index-plan-TEMPLATE-2026-07-15.md:159-161 | The unit JSON is specified as a bare list of key names — constructs[], measures[], readings[], concepts[], interlocutors[], tensions[], bridges{} — with zero element-level schema. No required per-element id, no id-uniqueness or namespacing rule, no required lo | **Addressed** — As C-10 |
| C-14 | CRITICAL | plans/vr-pedagogy-secondary-cluster-index-plan-2026-07-15.md:31-32 | The only quotation rule in the plan is a COPYRIGHT-LENGTH rule: no 25-word-or-longer verbatim, paraphrase plus 12-word-or-shorter anchor phrases with page numbers (echoed at gate line 221 and template:44-45). There is no fidelity requirement anywhere: nothing  | **Addressed** — B1/B2/C8: quotation machine-verification gated on match_kind=="exact" replaces the copyright-length-only rule; Decision 1 separates storage from display |
| C-15 | CRITICAL | plans/boredom-experiment-channel-schema-anchor-plan-2026-07-16.md:110-112 | The O-9 gate declares ch-eeg/.mat, ch-hmd telemetry, ch-obs video and ch-fig 'declared-but-unprocessed metadata only', and no instruction anywhere in the three plans says what must happen to the entry when that gate later clears. There is no re-open trigger, n | **Addressed** — C1 volatility field ({is_volatile, depends_on, resolved_at}) turns gate-state changes into a work queue; D3/D4 catch stale assertions post-change |
| C-16 | CRITICAL | plans/vr-pedagogy-secondary-cluster-index-plan-2026-07-15.md:118-127 | The plan hand-authors its frozen canonical vocabulary with TRAILING PARENTHETICAL QUALIFIERS -- construct rows 'presence (spatial/physical)', 'immersion (technological)', 'cognitive load (extraneous vs germane)' (lines 119-122) and media-condition columns 'IVR | **Addressed** — §4.5 TERM admission gate + C1 vocabulary rules (no trailing parentheticals in canonical vocab); D1 check-lemmas |
| C-19 | CRITICAL | plans/vr-pedagogy-secondary-cluster-index-plan-2026-07-15.md:194-205 | Section 6 defines compiler wiring as adding ONE TEXT_DIRS dict entry with ontology_format header and analysis_subdir _synthesis. That single entry causes the compiler to read exactly one file, _synthesis/cluster-ontology.md (compile-corpus-index.py:1217-1227). | **Addressed** — As C-07/C-11 (reachability contract; the one-file-read wiring assumption is retired with TEXT_DIRS) |
| C-21 | CRITICAL | plans/boredom-experiment-channel-schema-anchor-plan-2026-07-16.md:158-168 | §2.4 'The anchor's OUTBOUND edges (the load-bearing relationships)' names three relations but never says which file they go in, what the row/object schema is, or how the compiler ingests them. The plan does not mention `tension-edges.json` or `*-edges.csv` any | **Addressed** — C1: outbound edges get file, schema and ingestion path (EDGE records); count-only specification illegal |
| C-22 | HIGH | plans/boredom-experiment-channel-schema-anchor-plan-2026-07-16.md:101-105 | The PII gate says: "Do NOT `ls`/`find` inside `Subjects/` — a raw listing prints names into the transcript before any scrub; take structure/counts from `vle-02`/the brief." This conflates a transcript-hygiene concern with a repository-contents concern, and in  | **Addressed** — D1: the PII gate becomes an executable check over corpus files (S01–S08 grep), separated from transcript hygiene; permanent PII rule carried unchanged |
| C-23 | CRITICAL | plans/boredom-experiment-channel-schema-anchor-plan-2026-07-16.md:183-186 | The Phase-2 GATE has five items: (a) show the channel taxonomy, (b) show outbound edges + repointed bridges, (c) TEXT_DIRS diff + compiled node counts before→after, (d) grep the entry for the 8 names → empty, (e) bex-01 carries only aggregates + 4 cases. Only  | **Addressed** — D1: every gate is a named executable with threshold and exit code; narration cannot satisfy any gate |
| C-24 | CRITICAL | plans/vr-pedagogy-secondary-cluster-index-plan-2026-07-15.md:129-130 and :218-219 | Section 3 defines the concordance as the cluster's whole payoff (line 112 calls it the primary-object anchor; lines 113-114 say the lookup Part III needs is which construct behaves how under which medium) and specifies each cell as source(s) treating that cons | **Addressed** — §7 Phase-3: the concordance is a generated view over claim records, every cell resolving to a claim_id; D1 unbacked-assertions gate = 0 |
| C-25 | CRITICAL | plans/boredom-experiment-index-entry-plan-2026-07-10.md:50-54 | The only statement about the runtime in v1 (repeated as v2 §1 :58-62 and anchor §2.3 :154-156) says registration = add a TEXT_DIRS entry, then the parser reads §3A of the ontology file. It never states the corollary: that every OTHER file in the entry is invis | **Addressed** — As C-07 (the registration=visibility assumption is exactly what §11’s reader-table contract falsifies) |
| C-30 | CRITICAL | plans/secondary-cluster-index-plan-TEMPLATE-2026-07-15.md:114-124 | The layout mandates thirteen `_synthesis` artifacts and pairs six of them as `.{md,json}` twins. `compile-corpus-index.py:1216-1228` opens exactly one file per entry — `book-level-ontology.md` else `cluster-ontology.md` — plus `tension-edges.json`. Every `.jso | **Addressed** — As C-07/C-08 — commissioned artifacts must name their reader (file+function) or fail (D1 reachability + waiver rule) |
| C-32 | CRITICAL | plans/secondary-cluster-index-plan-TEMPLATE-2026-07-15.md:116 | `debate-map.{md,json}` and the tension layer are specified only as '8-12 documented cross-source disagreements, each with the sources on each pole + a resolution note'. No key names, no types, no id format. `compile-corpus-index.py:1128` hard-requires `node_a` | **Addressed** — C1 TENSION/debate schemas with key names, types, id format; count-only specs illegal |
| C-35 | MEDIUM | plans/secondary-cluster-index-plan-TEMPLATE-2026-07-15.md:116 and :173 (invoked by plan:185, gated a | global-edges.csv is specified only as all per-unit edges concatenated and deduped (line 116) and as concat plus dedup (line 173), and the gate counts deduplicated global edges (plan line 216). The DEDUPLICATION KEY is never stated. Neither plan says which colu | **Addressed** — As C-02 (dedup key stated; locus/note evidence-bearing columns never merged away) |
| C-36 | CRITICAL | plans/secondary-cluster-index-plan-TEMPLATE-2026-07-15.md:199 and :228 | Both the Phase-5 procedure and the §8 checklist require 're-run the repo's verbatim-quote detector' and 'verbatim detector clean'. No such tool exists. `ls scripts/` (107 entries) contains no verbatim, quote, or copyright checker; the only 'verbatim' code in s | **Addressed** — B1–B4 build the verbatim checker the plans referenced but never had; D1: a gate whose executable is missing FAILS |
| C-37 | CRITICAL | plans/uncomfortable-situations-2017-analysis.md:171 | The only specification of the position/claim record in the whole per-book plan family is one line: "`positions`: list of {claimant, claim, support, contested_by}". No id, no locus, no stance, no tier, no provenance tag. | **Addressed** — C1 CLAIM schema (id, locus, stance, tier required) replaces the one-line positions spec |
| C-38 | HIGH | plans/secondary-cluster-index-plan-TEMPLATE-2026-07-15.md:159-161 | The unit JSON is specified as a list of top-level key NAMES with no element schema, no required fields, no types, and no id requirement: `{... constructs[], measures[], readings[], concepts[], interlocutors[], tensions[], bridges:{<anchor-key>:[...]}, extracti | **Addressed** — As C-10/C-12 |
| C-40 | HIGH | plans/secondary-cluster-index-plan-TEMPLATE-2026-07-15.md:169-172 | The plan mandates node headers in the form `#### N. Name` and calls the format 'header format', 'replicate the Boredom/FCM structure exactly'. It never states that the header parser treats a trailing parenthetical as a Greek/German lemma. `compile-corpus-index | **Addressed** — As C-16 (the parenthetical-in-heading trap is closed by schema + check-lemmas, not authoring convention) |
| C-41 | CRITICAL | plans/in-game-calleja-2011-analysis.md:211 and plans/uncomfortable-situations-2017-analysis.md:199 ( | Tension edges are commanded by COUNT with no schema: "Tensions output: `tension-edges.json` (target 10–14)" / "Tensions split into `tension-edges.json` (target ≥8)". No plan states the key pair the compiler requires. scripts/compile-corpus-index.py:1125-1129 a | **Addressed** — As C-08/C-32 — the strongest single argument for schemas-not-counts (draft §3.3), adopted |
| C-42 | HIGH | plans/secondary-cluster-index-plan-TEMPLATE-2026-07-15.md:168-172 ("Replicate the Boredom/FCM struct | The plan mandates the header node format `#### N. Name` + `- **key**: value` and tells authors to copy a Heidegger/Greek exemplar verbatim, without ever stating what the parenthetical in the heading MEANS. scripts/compile-corpus-index.py:688-736 parses `^(.+?) | **Addressed** — As C-16/C-40 |
| C-43 | CRITICAL | plans/secondary-cluster-index-plan-TEMPLATE-2026-07-15.md:152-165 | The deepest record the plan ever asks an author to create is the UNIT — one per source, chapter or extracted paper. Every claim-bearing structure is a bare array name nested inside that unit JSON (:159-161 `readings[], concepts[], interlocutors[], tensions[]`) | **Addressed** — C1 CLAIM/CLAUSE granularity + Decision-2 density band; the unit stops being the deepest record (the plan’s core) |

### Refuted

| ID | Claim as filed | Why it was refuted | Refutation record |
|---|---|---|---|
| C-01 | The compiler cannot fail. compile-corpus-index.py main() has no exit-code path and no error accumulation; every malformed input is either warn()-ed to stderr and skipped or silently dropped. Criticall | Causal inversion: the template post-dates all three cited clusters by 1–2 months; the compiler no-exit posture is real but not caused by this plan line | Adversarial review §6.1; raw/workflow-journals/C-plan-review.journal.jsonl |
| C-13 | The plan states that cluster-ontology.md MUST carry the header-format Canonical Node List and calls it the compiler input (echoed at template:114 and :168-172). This asserts that the 3A heading scopes | The causal chain breaks at the product end (the compiler claim is true; the plan-to-defect link is not) | Adversarial review §6.1; raw/workflow-journals/C-plan-review.journal.jsonl |
| C-17 | d | Harness artifact: cites non-existent plans/x.md with defect “d”, consequence “c” — no verifiable claim was filed; retained in the record, not deleted | Adversarial review §6.1; raw/workflow-journals/C-plan-review.journal.jsonl |
| C-18 | The plan redefines the deliverable as a node: "Builds … as a compact primary-object entry — the lab/VR pole. Its value is the node itself + its outbound edges to the phenomenology; it is NOT a binding | Line quoted accurately; plan predates entry; but the causal chain fails — the correct narrower defect at :142 is recorded instead | Adversarial review §6.1; raw/workflow-journals/C-plan-review.journal.jsonl |
| C-20 | Section 4 mandates reproducing loadCorpusIndexContext at caps 12 ontology nodes / 3 hooks / 5 tensions with exact render templates, and :138 makes a line-for-line parity diff against the live TS the H | Temporal impossibility: the migration spec post-dates both the defect and its fix by ~9 weeks | Adversarial review §6.1; raw/workflow-journals/C-plan-review.journal.jsonl |
| C-26 | The usage plan makes concept-matrix.csv and *-edges.csv the primary entry points (:58 'Open concept-matrix.csv, scan the row for aisthesis'; :402 the file-type table) without defining a single column. | Causal chain fails at every tested link, including a grep-the-whole-plan miss | Adversarial review §6.1; raw/workflow-journals/C-plan-review.journal.jsonl |
| C-27 | Section 2C tells the writer to 'Browse tensions -> tension-edges.json (Aristotle), tension-edges.json (B&T), etc.' and the file-type table :399 describes tension-edges.json only as 'Argument structure | Quotes accurate; the inference is not — the chain from usage-plan lines to the product defect does not close | Adversarial review §6.1; raw/workflow-journals/C-plan-review.journal.jsonl |
| C-28 | The defer/special adapter row explicitly proposes deciding import-vs-defer for Dissertation, Veri(dis)similitude (Salvo MA), Game-Behavior Method, Uncomfortable Situations and Heidegger&Rhetoric on th | Never executed — no product surface exists for the spec’s defer/special adapter row to have damaged | Adversarial review §6.1; raw/workflow-journals/C-plan-review.journal.jsonl |
| C-29 | Section 4 mandates reproducing the MANDATORY THEORETICAL SYNTHESIS render verbatim -- '**Bridge [id]:** src (srcAuthor) <-> tgt (tgtAuthor)' plus 'Both authors MUST be cited with direct textual eviden | The runtime defect is real (independently reproduced) but the plan did not and cannot have caused it | Adversarial review §6.1; raw/workflow-journals/C-plan-review.journal.jsonl |
| C-31 | The plan asserts cross-pipeline hooks are pre-built, trustworthy bridges -- ':82 Each pipeline\'s cross-pipeline hooks file is a pre-built bridge. You don\'t need to re-read the primary texts to find  | Plan lines say what is claimed and the artifact is damaged — but the causal chain runs the wrong direction on three grounds | Adversarial review §6.1; raw/workflow-journals/C-plan-review.journal.jsonl |
| C-33 | tension-edges.json is the compiler's only edge channel (compile-corpus-index.py:1254-1273, requiring node_a/nodeA and node_b/nodeB per entry at :1125-1129). The word 'tension-edges' appears 0 times in | The plan post-dates every file it supposedly damaged; grep for tension-edges on the template returns zero hits | Adversarial review §6.1; raw/workflow-journals/C-plan-review.journal.jsonl |
| C-34 | 'Page offset (inferred, single-page scan): book_page = pdf_page - 2 ... Verify with 2 more anchors in Phase 0; if confirmed, never recompute per page.' The 'never recompute' instruction converts a two | Six failure modes checked, five fire — temporally impossible attribution among them | Adversarial review §6.1; raw/workflow-journals/C-plan-review.journal.jsonl |
| C-39 | The plan instructs authors to emit `cluster-ontology.{md,json}` / `book-level-ontology.{md,json}` — a JSON twin the compiler never opens. scripts/compile-corpus-index.py:1216-1227 reads only `book-lev | Mis-citation (template:113 is phase0-overview) plus chronology; four of six failure modes fire | Adversarial review §6.1; raw/workflow-journals/C-plan-review.journal.jsonl |

## Workflow D — archon-first assessment

### Confirmed

| ID | Sev | Location / scope | Finding (as filed) | Disposition |
|---|---|---|---|---|
| D-01 | FAVOURS-ARCHON | — | (c) The char_start/char_end exposure is a 4-file, ~10-line change and requires ZERO new plumbing: fragment_for already receives the chunk-local (lo,hi) as its `local_range: Option<(usize,usize)>` parameter and already destructures it two lines above the struct | **Addressed** — B3 implements exactly this (4-file ~10-line change; local_range already reaches fragment_for) |
| D-02 | FAVOURS-ARCHON | — | archon-evidence/src/index.rs is a complete, tested reader over the artifact's exact 4-array shape: it parses ontologyNodes/canonicalTerms/tensionEdges/crossPipelineHooks, does greedy longest-match canonical-term expansion, tension-pole surfacing, and owning-wo | **Addressed** — A8/C: archon’s tested index reader is the consumption path for the transferred index (design basis) |
| D-03 | RISK | — | archon's own queries all use that full-scan form: 24 `= $param` filter lines in archon-docs/src/store.rs alone. | **Addressed** — C1 query-plan hygiene requirement (key-prefix forms on all bulk paths; 24 full-scan sites documented) |
| D-05 | RISK | — | Cozo executes a stored-relation atom that is FIRST in a rule body as a full scan with a post-filter — there is no prefix pushdown for the `*rel{...}, key = $param` form archon uses everywhere. | **Addressed** — As D-03 (no prefix pushdown for first-atom stored relations — bulk queries must be written around it) |
| D-06 | FAVOURS-ARCHON | — | doc_chunk_blocks CONTAINS ROWS: 114,612 rows / 26,360,674 bytes / 230 B per row. doc_chunk_page_breaks has 27,227 rows. Both are live, not empty DDL. | **Addressed** — Design basis for C3 grounding (doc_chunk_blocks live at 114,612 rows); AR4 marks it a spot figure to re-measure |
| D-07 | FAVOURS-ARCHON | — | The two repos ALREADY hold divergent copies of compiled-index.json — archon's is 4 days stale and 31 nodes / 59 canonical terms behind. Hand-copying is the current sync mechanism and it has already drifted. | **Addressed** — A7 (manifest-verified transfer replaces hand-copy sync) + A8 (staleness made loud) |
| D-08 | FAVOURS-ARCHON | — | The sub-span-narrowed bbox is genuinely driven by doc_chunk_blocks, confirming the code path end-to-end on live data. | **Addressed** — Design basis for B3/C3 (sub-span bbox path confirmed end-to-end live) |
| D-09 | RISK | — | `archon evidence curate` produces exactly two JSON files on disk and writes NOTHING to the store. The curated set is assembled into an FCDP Pack + QuoteBank and written to `<out>/pack.json` and `<out>/quotes.json`; there is no CozoDB relation for a curated quo | **Deferred** — Deferred — curation persistence (curate writes only pack.json/quotebank.json, nothing to the store) is drafting-pipeline work; schedule with FCDP integration after Phase C schemas exist |
| D-10 | FAVOURS-ARCHON | — | CR-1/CR-2 (docs delete/reprocess prefix-resolving to the whole corpus with no gate) are FIXED: the default resolver is exact-only; prefix expansion is an explicit `--all-matching` opt-in. | **Addressed** — No action needed — CR-1/CR-2 confirmed fixed (exact-only resolver; --all-matching opt-in); recorded as design basis |
| D-13 | FAVOURS-ARCHON | — | The Bekker/PageNumber FragmentLocator — the citation-grade locus the corpus already captures from running heads — is dropped at the curation bridge and never reaches a pack. | **Addressed** — B3 downstream-plumbing note: FragmentLocator (Bekker/page) survives QuoteFragment→CuratedQuote→QuoteEntry with B1 |
| D-14 | FAVOURS-ARCHON | — | Exact character offsets ARE computed on the drafting path and are then thrown away because no struct has a field for them. The provenance loss is a field-width problem, not a capability gap. | **Addressed** — B3 (the field-width problem is the fix target) |
| D-15 | FAVOURS-ARCHON | — | archon already computes the exact char offsets and already USES them to narrow page and bbox — the discard is purely a missing struct field, so the data has zero acquisition cost. | **Addressed** — B3 (zero acquisition cost confirmed — compute exists, field missing) |
| D-17 | FAVOURS-ARCHON | — | The pack's `intended_use` is a hardcoded constant and `description` and `evidence` are the same string, so judge gate G-E item E4 evaluates every RA-curated pack against nothing. | **Deferred** — Deferred — FCDP judge-gate quality (hardcoded intended_use makes E4 vacuous); schedule with drafting-gauntlet work, not the index overhaul |
| D-18 | FAVOURS-ARCHON | — | CR-3 (the verbatim-gate critical: verify_bank located text corpus-wide and never bound to the cited source) is FIXED on current HEAD, with two regression tests naming the failure mode. | **Addressed** — No action needed — CR-3 verbatim-gate fix confirmed on HEAD with regression tests; B1 builds on it (design basis) |
| D-20 | FAVOURS-ARCHON | — | Something DOES already resemble a claim record with a locus: `ReviewItem`. It carries a one-line claim (`note`), a typed locus (`locus`, `cite`), the source, the collection, the provenance trust string, `page`, `bbox`, `chunk_id`, and `page_boxes` (every locat | **Addressed** — Design input to C1 (ReviewItem is the closest existing claim-record shape; schema derives from it where fields fit) |
| D-21 | FAVOURS-ARCHON | — | verify.rs verifies a pack's quote bank against the LIVE corpus, source-scoped to the document each quote's p4a entry cites. It resolves the cited source label to an ingested document_id (refusing to guess on ambiguity), runs `find_fragment_bboxes` in that docu | **Addressed** — Design basis for B1 (verify.rs blocking() is the gate B1 moves to the engine boundary) |
| D-23 | RISK | — | The two copies are 98.2% identical and drifting. The archon copy is 26 files and 10 content-diffs behind; nothing exists only in archon. | **Addressed** — A7 (the 98.2%-identical drifting copies end; one-way ownership declared) |
| D-25 | FAVOURS-ARCHON | — | Live quote verification with sub-span page, bbox and Bekker locator works today, and correctly discriminates exact from fuzzy. | **Addressed** — Design basis for B2/C8 (live discrimination of exact vs fuzzy confirmed) |
| D-26 | FAVOURS-ARCHON | — | archon already reads the index in production and returns provenance the incumbent artifact cannot express — canonical-term expansion, tension surfacing, owning-work boost, exact-match bbox and Bekker locators. | **Addressed** — Design basis for A8/C (archon reads the index in production; provenance the incumbent artifact cannot express) |
| D-28 | RISK | — | The always-true bridge matcher is confirmed empirically at exactly 31 of 72 hooks, and its cause is an empty targetConcept making `t.includes(sourceLower)` vacuously true. This is load-bearing bug-compatibility: a 'cleaner' emitter that fills these in changes  | **Deferred** — Deferred — bug-compatibility requirement (31 empty targetConcept values) documented in the legacy-view emitter contract (archive synthesis §6); the emitter itself is deferred until the Python compiler is retired |
| D-31 | RISK | — | A second, undocumented bug-compatibility trap: hook tag confidence ranking tests for the substring 'medium', but the live tag values are 'INTERP-med'. So INTERP-med and INTERP-low both score 0 and are indistinguishable in the sort. Normalizing tags to 'INTERP- | **Deferred** — Deferred — as D-28 (INTERP-med substring trap documented in the same contract) |
| D-33 | FAVOURS-ARCHON | — | CORRECTION to the given facts: archon's quote verification is a content check, not a date. The `Verification{quotes_verified_at}` field in archon-draft is pack metadata, but crates/archon-evidence/src/verify.rs is a real re-verification gate that blocks on any | **Addressed** — No action needed — correction recorded: verification is a content check (verify_bank), not a date field; design basis for B1 |
| D-36 | FAVOURS-INCUMB | — | The duplicated compiled-index.json in archon is stale by four days and ~21 KB, with a different checksum. Sync is manual and undated by any automation. | **Addressed** — A7/A8 (stale duplicate replaced; staleness loud) |
| D-40 | FAVOURS-ARCHON | — | Sub-span page, bbox and Bekker locator resolution works live, not just in schema. | **Addressed** — Design basis for B3/C8 (sub-span page/bbox/Bekker live) |

### Refuted

| ID | Claim as filed | Why it was refuted | Refutation record |
|---|---|---|---|
| D-04 | Full relation-scan and lookup costs at current scale are small: 2,475,077-row FTS relation scanned in 80 ms; 114,612-row doc_chunk_blocks in 3.84 ms; 56 MB of doc_chunks values in 14.8 ms; point looku | Three of the five figures measure covering-index counts, not scans; value reads cost 2,543 ms (32× worse) — the corrected numbers drive §15.3 and C3’s grounding design | Archon-first synthesis §4 + 15-ARCHON-FIRST-VERDICT §15.2; raw/workflow-journals/D-archon-first.journal.jsonl (final 40 verifier records) |
| D-11 | The FCDP drafting protocol has NO implementation in claudeflow-testing. The only FCDP code there is a 43-line substitution script; the whole gate-enforced pipeline exists solely in archon-cli. The use | find -name "*.py" could not match the .ts/.md counter-evidence; FCDP is fragile-and-untracked, not absent | Archon-first synthesis §4 + 15-ARCHON-FIRST-VERDICT §15.2; raw/workflow-journals/D-archon-first.journal.jsonl (final 40 verifier records) |
| D-12 | CORRECTION to the established facts: a content re-check against the live corpus DOES exist on the `archon draft` path — it is just not in gp_validate. verify_bank blocks the run on any non-Exact statu | The gate is not on the archon draft path as scoped — one of five entry points only (drives B1) | Archon-first synthesis §4 + 15-ARCHON-FIRST-VERDICT §15.2; raw/workflow-journals/D-archon-first.journal.jsonl (final 40 verifier records) |
| D-16 | The index path is live-wired into retrieval, not shelf-ware: an index-expanded query returns candidates with index_boost, primary-source flagging, and exact provenance (page + bbox + match_kind). | Retrieval half true; “exact provenance” is a tautology (evidence find re-anchors chunk against own content); bbox coverage 68% not universal (drives E3 and the §5 degradation rule) | Archon-first synthesis §4 + 15-ARCHON-FIRST-VERDICT §15.2; raw/workflow-journals/D-archon-first.journal.jsonl (final 40 verifier records) |
| D-19 | The corpus/index tree is ALREADY inside archon-cli and is better version-controlled there than in the incumbent. archon-cli tracks 1,731 index files in git across 24 of 27 entry dirs; claudeflow-testi | Counts right, conclusion wrong: archon’s tracked copy is stale, missing three entries, zero readers — “better version-controlled” does not hold | Archon-first synthesis §4 + 15-ARCHON-FIRST-VERDICT §15.2; raw/workflow-journals/D-archon-first.journal.jsonl (final 40 verifier records) |
| D-22 | Only two loaders read the artifact from disk, and one of them bypasses the shared loader entirely, resolving against process.cwd() rather than the caller's projectRoot — so the two can disagree when c | Descriptive observations real; the load-bearing risk consequence is unreachable in the live codebase; reader count wrong | Archon-first synthesis §4 + 15-ARCHON-FIRST-VERDICT §15.2; raw/workflow-journals/D-archon-first.journal.jsonl (final 40 verifier records) |
| D-24 | archon does NOT need the tree to move. Its only path assumption is one cwd-relative const; the loader is already path-generic, an absolute-path env hook already exists for tests, and file reads are un | Loader is path-generic, but “archon does not need the tree to move” fails as a cost claim (and is mooted by the transfer directive) | Archon-first synthesis §4 + 15-ARCHON-FIRST-VERDICT §15.2; raw/workflow-journals/D-archon-first.journal.jsonl (final 40 verifier records) |
| D-27 | The 157 tsc errors do NOT touch the index path. Every index module typechecks clean; the errors cluster in section-orchestrator, __experimental__/, and style-drift code. quality-integration.ts's 4 err | The four checkable sub-facts reproduce; the generalisation “157 errors do not touch the index path” is false as worded | Archon-first synthesis §4 + 15-ARCHON-FIRST-VERDICT §15.2; raw/workflow-journals/D-archon-first.journal.jsonl (final 40 verifier records) |
| D-29 | CONFIRMED: 157 tsc errors, so the incumbent's TypeScript cannot build. package.json:13 defines build as `tsc`. | Numbers reproduce; the operative conclusion (broken build motivates the port) fails — zero errors in the index layer | Archon-first synthesis §4 + 15-ARCHON-FIRST-VERDICT §15.2; raw/workflow-journals/D-archon-first.journal.jsonl (final 40 verifier records) |
| D-30 | A build-ready, adversarially-reviewed spec for this exact port (corpus/index → archon CozoDB) has existed since 2026-06-24 and has produced zero implementation in 35 days. This is the single best avai | Spec-exists-with-zero-lines confirmed; “single best predictor” inference refuted | Archon-first synthesis §4 + 15-ARCHON-FIRST-VERDICT §15.2; raw/workflow-journals/D-archon-first.journal.jsonl (final 40 verifier records) |
| D-32 | archon has a populated, indexed provenance store; claudeflow-testing has none. | Part one true (archon store populated); part two false — the comparative’s load-bearing half fails | Archon-first synthesis §4 + 15-ARCHON-FIRST-VERDICT §15.2; raw/workflow-journals/D-archon-first.journal.jsonl (final 40 verifier records) |
| D-34 | The migration spec's own decisive framing states that Rust/CozoDB buys nothing on the critical path — the long pole is adapter coverage over irregular hand-authored markdown, which is precisely what t | Quotes real (one line error); every inferential step built on them fails | Archon-first synthesis §4 + 15-ARCHON-FIRST-VERDICT §15.2; raw/workflow-journals/D-archon-first.journal.jsonl (final 40 verifier records) |
| D-35 | The incumbent's compiled index cannot carry per-claim provenance: not one of its 701 node records has an id, a page, or a quote field. | Measurement exactly right (no id/page/quote in any node); the inference and stated bearing are backwards | Archon-first synthesis §4 + 15-ARCHON-FIRST-VERDICT §15.2; raw/workflow-journals/D-archon-first.journal.jsonl (final 40 verifier records) |
| D-37 | The two-repo split already exists and has already silently lost three complete index entries. archon-cli/index/ is a checked-in duplicate of corpus/index/ that is missing 25 files across three entries | File counts check out; “already silently lost” is false — nothing exists only in archon; the bearing inference fails | Archon-first synthesis §4 + 15-ARCHON-FIRST-VERDICT §15.2; raw/workflow-journals/D-archon-first.journal.jsonl (final 40 verifier records) |
| D-38 | archon's store contains no index-entry content at all — only source PDFs. The overhaul's subject matter (the hand-authored analytical layer) has zero presence in the proposed target. | Sentence one accurate; the load-bearing second sentence false in the opposite direction from what it implies | Archon-first synthesis §4 + 15-ARCHON-FIRST-VERDICT §15.2; raw/workflow-journals/D-archon-first.journal.jsonl (final 40 verifier records) |
| D-39 | The artifact is perfectly uniform: exactly one key order per record type across all 837 records, all scalars are strings, no nulls, no missing keys, units/aliases always list[str]. This makes a byte-i | Key-order facts true (reproduced on two artifact generations); the claim’s bearing as filed did not survive | Archon-first synthesis §4 + 15-ARCHON-FIRST-VERDICT §15.2; raw/workflow-journals/D-archon-first.journal.jsonl (final 40 verifier records) |

---

## Step-reference key

P0.x = Phase 0 preservation · A1–A13 = Phase A (A2 compiler elif fix; A3 bridge-fabrication trio; A4 ig-10; A5/A6 wraith removal; A7 transfer; A8 index-path+staleness; A9 loud loads; A10 truncation guards; A12 chunkId dedupe; A13 authority_tier) · B1–B4 = verification path · C1–C8 = schema/imports (C5 relational apparatus; C7 reasoning edges; C8 corpus-wide quote re-anchoring) · D1–D5 = creation-system lock (D3 post-recompile assertion; D4 reverse-reference index + sweep) · E0–E4 = ingestion hardening (E1 pypdfium2) · F1–F3 = re-ingest/regenerate (F2 carries the per-entry defect checklist derived from this matrix). §4.5/§7/§11 = sections of the draft plan adopted as specification of record by 01.

